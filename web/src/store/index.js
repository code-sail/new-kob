import { createStore } from 'vuex'
import ModuleUser from './user' //随便起个名字代表user.js就行

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    user: ModuleUser,
  }
})
